# Fixture data for AdminUser (ActiveAdmin/Devise).
# Note: the generator's empty fixtures break the unique email index; keep
# emails and encrypted passwords distinct per fixture.
# == Schema Information
#
# Table name: admin_users
#
#  id                     :bigint           not null, primary key
#  email                  :string           default(""), not null
#  encrypted_password     :string           default(""), not null
#  remember_created_at    :datetime
#  reset_password_sent_at :datetime
#  reset_password_token   :string
#  created_at             :datetime         not null
#  updated_at             :datetime         not null
#
# Indexes
#
#  index_admin_users_on_email                 (email) UNIQUE
#  index_admin_users_on_reset_password_token  (reset_password_token) UNIQUE
#
one:
  email: one@example.com
  encrypted_password: <%= Devise::Encryptor.digest(AdminUser, "password") %>

two:
  email: two@example.com
  encrypted_password: <%= Devise::Encryptor.digest(AdminUser, "password") %>
